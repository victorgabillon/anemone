# Tests

Tests focus on exploration index behavior and tree construction.

- `test_indices.py` exercises index computation and updates against YAML-based
  fixtures.
- `fake_yaml_game.py` defines a simple state implementation for fixtures.
- `data/` holds YAML trees used by the tests.
