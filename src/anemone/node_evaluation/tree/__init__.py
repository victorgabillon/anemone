"""Tree-evaluation families built on top of shared backup/runtime helpers.

This subpackage is where backed-up values, branch ordering, frontier tracking,
and principal-variation updates live. Public factory entrypoints are exposed
from :mod:`anemone.node_evaluation.tree.factory`.
"""
