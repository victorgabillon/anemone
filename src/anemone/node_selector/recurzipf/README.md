# RecurZipf selector

RecurZipf selectors walk down the current best line using Zipf sampling priorities
and then open branches at the selected leaf.

See `recur_zipf_base.py` for the base implementation and argument definition.
