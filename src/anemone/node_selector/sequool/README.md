# Sequool selector

Sequool selectors focus on depth-based exploration. They track not-yet-opened
nodes by depth and decide which depth to expand next, optionally using recursive
selection over the full tree.

See `factory.py` and `sequool.py` for configuration and implementation details.
