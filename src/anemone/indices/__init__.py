"""Exploration-index packages for search-time node prioritization.

High-level roles:

* :mod:`anemone.indices.node_indices` defines the per-node exploration payloads
* :mod:`anemone.indices.index_manager` coordinates strategy selection and index
  recomputation across a tree
"""
